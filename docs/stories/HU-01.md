# HU-01: Registro por foto con IA (completar)

> **Sprint:** 3 · **Etiqueta:** ⚡ Quick Win · **Esfuerzo:** 4 días-persona · **Branch:** `feat/HU-01-photo-confidence-ui`

---

## Objetivo
Completar la funcionalidad de foto IA con resaltado visual de confianza baja, guía de encuadre y enforcement de timeout. Las cuotas free/premium se difieren al sprint de Billing.

---

## Decisiones aplicadas
- **D3:** Cuotas Gemini diferidas a sprint de Billing futuro
- Implementar visual + UX completo SIN sistema de cobros

---

## Estado previo (PARCIAL)
- ✅ Cámara y galería con normalización EXIF + resize 1024px
- ✅ Gemini 2.5 Flash retorna ítems con `confianza` (0–1)
- ✅ StepConfirm permite editar peso + eliminar ítems
- ✅ Fallback a entrada manual
- ✅ Confianza guardada en `Meal.confidence`
- ❌ Sin guía visual de encuadre
- ❌ Sin resaltado de ítems con baja confianza
- ❌ Sin timeout claro (UX)
- ⏸️ Cuotas diferidas (post Sprint 12)

---

## Archivos a crear

| Archivo | Propósito |
|---|---|
| `app/components/add/CameraGuide.tsx` | Overlay con guía de encuadre (rule of thirds) |
| `app/components/add/ConfidenceBadge.tsx` | Indicador visual de confianza alto/medio/bajo |
| `__tests__/components/StepConfirm.test.tsx` | Tests del resaltado de confianza |
| `e2e/HU-01-photo-flow.spec.ts` | E2E completo del flujo foto IA |

---

## Archivos a modificar

| Archivo | Cambio |
|---|---|
| `app/components/add/StepCamera.tsx` | Renderizar `<CameraGuide>` sobre el preview |
| `app/components/add/StepConfirm.tsx` | Aplicar borde + icono warning a ítems con `confianza < 0.6` |
| `app/api/analyze/route.ts` | Agregar `AbortController` con timeout 8s explícito |

---

## Lógica nueva

### Categorización de confianza
```ts
type ConfidenceLevel = "high" | "medium" | "low";

function categorizeConfidence(c: number): ConfidenceLevel {
  if (c >= 0.8) return "high";
  if (c >= 0.6) return "medium";
  return "low";
}
```

### Tratamiento visual
- **High (≥0.8):** sin indicador (default)
- **Medium (0.6–0.8):** badge gris `Confianza media`
- **Low (<0.6):** borde amarillo del ítem + icono `AlertTriangle` + tooltip "Verifica este alimento"

### Timeout enforcement (8s)
```ts
// app/api/analyze/route.ts
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 8000);

try {
  const result = await model.generateContent(
    [{ inlineData: { ... } }, { text: prompt }],
    { signal: controller.signal }
  );
  return Response.json(json);
} catch (e) {
  if (e.name === "AbortError") {
    return Response.json(
      { error: "El análisis tardó demasiado. Intenta con una foto más simple o usa la búsqueda manual." },
      { status: 504 }
    );
  }
  throw e;
} finally {
  clearTimeout(timeoutId);
}
```

---

## Componentes UI

### `app/components/add/CameraGuide.tsx`
```tsx
export default function CameraGuide({ active }: { active: boolean }) {
  if (!active) return null;
  return (
    <div className="absolute inset-0 pointer-events-none flex flex-col items-center justify-center">
      {/* Marco cuadrado central con esquinas */}
      <div className="relative w-3/4 aspect-square">
        {/* Esquinas blancas */}
        <span className="absolute top-0 left-0 w-6 h-6 border-l-2 border-t-2 border-white/80" />
        <span className="absolute top-0 right-0 w-6 h-6 border-r-2 border-t-2 border-white/80" />
        <span className="absolute bottom-0 left-0 w-6 h-6 border-l-2 border-b-2 border-white/80" />
        <span className="absolute bottom-0 right-0 w-6 h-6 border-r-2 border-b-2 border-white/80" />
      </div>
      <p className="absolute bottom-6 text-white/90 text-xs uppercase tracking-[0.08em]">
        Encuadra el plato completo
      </p>
    </div>
  );
}
```

### `app/components/add/ConfidenceBadge.tsx`
```tsx
import { AlertTriangle, CircleCheck } from "lucide-react";
import Icon from "@/app/components/ui/Icon";

type Props = { confidence: number };

export default function ConfidenceBadge({ confidence }: Props) {
  if (confidence >= 0.8) return null;
  if (confidence >= 0.6) {
    return (
      <span className="inline-flex items-center gap-1 text-xs text-text-tertiary">
        Confianza media
      </span>
    );
  }
  return (
    <span className="inline-flex items-center gap-1 text-xs text-accent-warm" title="Verifica este alimento — confianza baja">
      <Icon icon={AlertTriangle} size={12} />
      Verificar
    </span>
  );
}
```

### Modificación de `StepConfirm.tsx`
```tsx
{items.map((item, idx) => {
  const lowConfidence = item.confianza < 0.6;
  return (
    <div
      key={idx}
      className={`p-4 rounded-xl border ${
        lowConfidence
          ? "border-accent-warm bg-accent-warm/5"
          : "border-border bg-bg-secondary"
      }`}
    >
      <div className="flex items-center justify-between">
        <p className="text-sm font-medium text-text-primary">{item.nombre}</p>
        <ConfidenceBadge confidence={item.confianza} />
      </div>
      {/* ... resto del item ... */}
    </div>
  );
})}
```

---

## Tests

### Unit / Component

**`__tests__/components/ConfidenceBadge.test.tsx`**
```ts
import { render, screen } from "@testing-library/react";
import ConfidenceBadge from "@/app/components/add/ConfidenceBadge";

it("shows nothing for high confidence", () => {
  const { container } = render(<ConfidenceBadge confidence={0.95} />);
  expect(container).toBeEmptyDOMElement();
});

it("shows 'Verificar' for low confidence", () => {
  render(<ConfidenceBadge confidence={0.4} />);
  expect(screen.getByText("Verificar")).toBeInTheDocument();
});

it("shows 'Confianza media' for medium", () => {
  render(<ConfidenceBadge confidence={0.7} />);
  expect(screen.getByText(/media/i)).toBeInTheDocument();
});
```

**`__tests__/components/StepConfirm.test.tsx`**
```ts
it("renders low confidence items with warning border", () => {
  const items = [
    { nombre: "Pollo", confianza: 0.9, pesoG: 100, calorias: 165, ... },
    { nombre: "Salsa misteriosa", confianza: 0.4, pesoG: 30, calorias: 50, ... },
  ];
  render(<StepConfirm items={items} ... />);
  expect(screen.getByText("Salsa misteriosa").closest("div")).toHaveClass("border-accent-warm");
});
```

### API

**`__tests__/api/analyze-timeout.test.ts`**
```ts
it("returns 504 when Gemini takes > 8s", async () => {
  vi.spyOn(genAI, "getGenerativeModel").mockReturnValue({
    generateContent: () => new Promise((r) => setTimeout(r, 10000)),
  });
  const res = await POST(mockRequest({ imageBase64: "..." }));
  expect(res.status).toBe(504);
});
```

### E2E

**`e2e/HU-01-photo-flow.spec.ts`**
```ts
test("flujo completo de foto con IA muestra guía de encuadre", async ({ page }) => {
  await loginAs(page, "user@test.com");
  await page.goto("/day/2026-05-22");
  await page.click('button[aria-label="Agregar comida"]');
  await page.click('button:has-text("Foto con IA")');
  await expect(page.locator("text=Encuadra el plato completo")).toBeVisible();

  // Subir mock photo
  await page.setInputFiles('input[type="file"]', "fixtures/plato.jpg");
  await page.click('button:has-text("Analizar con IA")');

  // Esperar resultados de mock Gemini
  await page.waitForSelector(".step-confirm");

  // Item con baja confianza debe tener borde de warning
  const lowConfidenceItem = page.locator(".border-accent-warm").first();
  await expect(lowConfidenceItem).toBeVisible();
});
```

---

## Criterios de aceptación → tests

| Criterio | Test |
|---|---|
| "cámara se abre con guía visual de encuadre" | E2E `Encuadra el plato completo` visible |
| "IA procesa en menos de 5 segundos" | ⚠️ Imposible enforcement client-side. Tenemos timeout en 8s del server. Para análisis típicos Gemini responde en 2-4s. KPI a monitorear post-merge. |
| "muestra alimentos con porción en gramos y nivel de confianza (alto/medio/bajo)" | `ConfidenceBadge.test.tsx` |
| "ítem con confianza baja se resalta visualmente y permite edición rápida" | `StepConfirm.test.tsx` → border + edit existente |
| "confirmo, se registra en diario y se entrena el modelo con correcciones" | Diario sí; feedback loop ML diferido a sprint futuro (out of scope) |

---

## Definition of Done

- [ ] `CameraGuide` se muestra durante captura
- [ ] Ítems con `confianza < 0.6` tienen borde naranja + badge "Verificar"
- [ ] Timeout 8s con 504 + mensaje claro al usuario
- [ ] 3+ tests component + 1 API + 1 E2E pasando
- [ ] PR con screenshots de los 3 estados (alto/medio/bajo)

---

## Riesgos específicos

| Riesgo | Mitigación |
|---|---|
| Gemini puede saturarse con muchas fotos sin cuota | Diferido a sprint de Billing; mientras tanto, monitor de costos en Vercel |
| Guía de encuadre puede confundir si la foto no es de un plato | Texto claro "Encuadra el plato completo" + skip implícito (no bloquea) |
| Umbral 0.6 puede ser muy bajo o muy alto | Ajustar con datos reales post-merge; sin breaking change |
| AbortController + Gemini SDK puede no soportar signal | Verificar en `@google/generative-ai` v0.24+; si no, usar `Promise.race` con setTimeout |

---

## Cuotas free/premium (DIFERIDO a Sprint Billing)

Cuando exista el sprint de Billing (post Sprint 12), agregar:
- Modelo `UsageEvent(userId, eventType: "gemini_photo", count, period)`
- Endpoint `/api/usage/check` antes de `/api/analyze`
- Free: 5 fotos/día; Premium: ilimitado
- UI: badge "X/5 fotos hoy" en home + upgrade modal cuando excede
- Email/notification al alcanzar el 80% del cuota

---

## Sub-PRs sugeridos

1. **PR-1 (feat):** `CameraGuide` + integración en `StepCamera`
2. **PR-2 (feat):** `ConfidenceBadge` + resaltado en `StepConfirm`
3. **PR-3 (fix):** AbortController timeout 8s
4. **PR-4 (test):** suite completa

---

## Métricas de éxito post-merge

- Tasa de edición manual post-IA reducida ≥20%
- 0 quejas de "la foto no se procesa nunca" (timeout funciona)
- Confianza promedio reportada ≥0.75 (proxy de calidad de fotos)
